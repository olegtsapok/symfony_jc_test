{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Photo edit</h1>

    <img src="/upload/{{ entity.id }}.{{ entity.extension }}?name={{ entity.name }}" width="150px">
    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('photo') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
