{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Discount list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Amount</th>
                <th>Quantityproducts</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('discount_show', { 'id': entity.id }) }}">{{ entity.amount }}</a></td>
                <td>{{ entity.quantityProducts }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('discount_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('discount_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('discount_new') }}">
                Create a new entry
            </a>
        </li>
        <li>
            <a href="{{ path('site') }}">
                Index
            </a>
        </li>
    </ul>
    {% endblock %}
